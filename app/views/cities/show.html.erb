<h1><%= @city.name %> - Your <%= @city.rank %> Ranked City</h1>

<h2>Edit City Rank</h2>
<form action="/cities/<%= @city.id %>" method="post">
  <label>Rank:</label>
  <input type="text" name="city[rank]" placeholder="1" value="<%= @city.rank %>">

  <button>Update City Rank</button>
</form>
